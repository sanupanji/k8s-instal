apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: example-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
        - path: /apple
          backend:
            service:
              name: nginx-lab
              port:
                number: 80
        - path: /banana
          backend:
            name: nginx-lab-x
              port:
                number: 80